PORT=
WHATSAPP_TOKEN=
VERIFY_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
APP_SECRET=
GOOGLE_SHEETS_WEBHOOK_URL=

# Runtime storage path for local files such as abuse.json.
# In production, this can point to a persistent disk mount.
# Do not store secrets here, and do not version the contents of data/.
DATA_DIR=./data

# Legacy/config aliases. Keep empty unless the runtime explicitly requires them.
PHONE_NUMBER_ID=
GOOGLE_SHEETS_URL=
META_VERIFY_TOKEN=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_API_VERSION=
